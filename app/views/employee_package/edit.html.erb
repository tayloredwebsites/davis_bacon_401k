<br><h2>Editing employee package</h2>

<% form_tag(:action => 'update', :id => @employee_package) do %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Save' %>
<% end %>

<%= link_to 'Edit Employee', :controller => 'employee', :action => 'show', :id => @employee_package.employee_id %> |
<%= link_to 'List Employees', :controller => 'employee', :action => 'list' %>
